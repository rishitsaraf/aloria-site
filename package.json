{
  "name": "aloria-site",
  "version": "1.0.0",
  "private": true,
  "description": "Aloria — storefront, brand hub and commerce API",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "playwright-core": "^1.62.1"
  },
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "test": "bash scripts/run-tests.sh",
    "test:smoke": "node tests/smoke.js"
  }
}
